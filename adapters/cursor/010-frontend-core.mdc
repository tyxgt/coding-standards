---
description: 前端编码规范核心（命名、组件、样式、TypeScript基础）
globs: "*.tsx,*.ts,*.jsx,*.js,*.less,*.css"
alwaysApply: true
---

# 前端编码规范核心 (v1.0.0)

## 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 页面目录 | PascalCase | `AlgorithmConfig/` |
| 组件目录 | camelCase | `formItem/` |
| 变量/函数 | camelCase | `userList`、`fetchData()` |
| 常量 | camelCase 或 UPPER_SNAKE | `pageSize`、`MAX_COUNT` |
| 布尔值 | is/has/should 前缀 | `isVisible`、`hasPermission` |
| 事件处理 | handle 前缀 | `handleClick`、`handleSearch` |
| 组件 | PascalCase | `UserTable` |
| Props 接口 | 组件名 + Props | `UserTableProps` |
| 文件入口 | `index.tsx` | 每个组件/页面目录 |
| Hook 文件 | `useXxx.ts` | `useAuth.ts` |

## 组件结构

```tsx
// 1. Props 类型（文件顶部）
interface UserTableProps {
  userId: string;
  onSelect?: (user: User) => void;
}

// 2. 函数组件 + 类型标注
const UserTable: React.FC<UserTableProps> = ({ userId, onSelect }) => {
  // 3. 状态在最前面
  const [data, setData] = useState<User[]>([]);

  // 4. 副作用紧随其后
  useEffect(() => { fetchUserData(userId); }, [userId]);

  // 5. handle 前缀事件处理
  const handleClick = (record: User) => { onSelect?.(record); };

  // 6. 主渲染
  return <div>...</div>;
};

export default UserTable;
```

- 单文件 ≤ 300 行，Props ≤ 8 个，useEffect ≤ 4 个
- 条件渲染：三元表达式（简单）或逻辑与（短路），避免嵌套三元
- 列表 key 使用唯一且稳定的值，不使用索引

## 样式规范

| 类别 | 规范 |
|------|------|
| 组件样式 | CSS Modules，camelCase 命名 |
| 全局样式 | kebab-case，放 global.less |
| 动态样式 | 仅动态计算值用 style 属性 |
| 选择器嵌套 | ≤ 3 层 |
| !important | 禁止使用 |

```tsx
import styles from './index.less';
<div className={styles.container} />
```

## TypeScript

- `strict: true`，禁止 `any`，用 `unknown` 替代
- Props 定义用 `interface`，联合/交叉类型用 `type`
- `import type` 用于类型导入（编译时移除）
- 禁止 `@ts-ignore`，用 `@ts-expect-error` + 注释
- 禁止使用 `!` 非空断言
- 禁止使用 `Function` 类型

## 通用禁止

- ❌ `var`、`any`、`@ts-ignore`
- ❌ 内联静态样式（`style={{ color: 'red' }}`）
- ❌ class 组件
- ❌ 直接修改 state
- ❌ 未使用的 import 或变量
- ❌ 循环依赖
- ❌ 嵌套三元表达式
- ❌ `console.log` 遗留到生产
