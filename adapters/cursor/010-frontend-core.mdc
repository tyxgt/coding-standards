---
description: 前端编码规范核心（命名、组件约束、样式方案、TypeScript禁止项）
globs: "*.tsx,*.ts,*.jsx,*.js,*.less,*.css"
alwaysApply: true
---

# 前端编码规范核心 (v2.0.0)

## 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 页面目录 | PascalCase | `AlgorithmConfig/` |
| 组件目录 | camelCase | `formItem/` |
| 文件入口 | `index.tsx` | 每个组件/页面目录 |
| Hook 文件 | `useXxx.ts` | `useAuth.ts` |

## 组件约束

- 单文件 ≤ 300 行，Props ≤ 8 个，useEffect ≤ 4 个
- 结构顺序：Props 类型 → 组件 → state → effect → 事件 → 渲染
- 默认导出组件，命名导出类型

## 样式规范

| 类别 | 规范 |
|------|------|
| 组件样式 | CSS Modules，camelCase 命名（`styles.container` ✅，`styles['kebab-case']` ❌） |
| 全局样式 | kebab-case，放 global.less |
| 方案选择 | Umi → Less + CSS Modules，Vite → CSS Modules/Tailwind |
| 选择器嵌套 | ≤ 3 层 |
| !important | 禁止使用 |

## TypeScript

- Props 定义用 `interface`，联合类型用 `type`
- `import type` 用于类型导入
- 禁止 `@ts-ignore`（用 `@ts-expect-error` + 注释）
- 禁止 `!` 非空断言，禁止 `Function` 类型

## 禁止

- ❌ `any`、`@ts-ignore`
- ❌ 内联静态样式
- ❌ 直接修改 state
- ❌ 循环依赖，嵌套三元
- ❌ `console.log` 遗留到生产
